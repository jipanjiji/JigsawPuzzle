{
  "rules": {
    ".read": "auth != null",
    "users": {
        "$uid": {
            ".write": "auth != null && auth.uid == $uid"
        }
    },
    "rooms": {
        ".read": "auth != null",
        ".write": "auth != null"
    }
  }
}